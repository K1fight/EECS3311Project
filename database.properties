# Database Configuration File
# Uncomment and modify the database you want to use

# SQLite (Default - Lightweight, file-based database)
db.type=sqlite
db.url=jdbc:sqlite:consulting_booking.db
db.username=
db.password=

# MySQL (For production use)
# db.type=mysql
# db.url=jdbc:mysql://localhost:3306/consulting_booking?useSSL=false&serverTimezone=UTC
# db.username=root
# db.password=your_password

# PostgreSQL (For production use)
# db.type=postgresql
# db.url=jdbc:postgresql://localhost:5432/consulting_booking
# db.username=postgres
# db.password=your_password

# Connection Pool Settings (Optional)
db.pool.initialSize=5
db.pool.maxSize=20
db.pool.timeout=30000
